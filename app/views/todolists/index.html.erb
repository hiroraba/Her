<h1>Listing todolists</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Content</th>
      <th>Priority</th>
      <th>Start</th>
      <th>end</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @todolist.each do |todolist| %>
      <tr>
        <td><%= todolist.title %></td>
        <td><%= todolist.priority %></td>
	<td><%= todolist.start.strftime("%Y-%m-%d") %></td>
	<td><%= todolist.end.strftime("%Y-%m-%d") %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<%= link_to 'New Todolist', new_todolist_path %>
<br>
<%= link_to 'view your todo on calendar', 'calendar/index' %>
